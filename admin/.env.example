# Copy to .env — base URL of the portfolio API (no trailing slash)
VITE_API_URL=http://localhost:5000/api

# Origin of the public site. Screenshot paths like /images/foo/bar.jpg are
# served by the frontend, so previews here are resolved against this.
VITE_SITE_URL=http://localhost:5173
